<%= form_with(model: authors_book) do |form| %>
  <% if authors_book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(authors_book.errors.count, "error") %> prohibited this authors_book from being saved:</h2>

      <ul>
        <% authors_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :authors_id, style: "display: block" %>
    <%= form.text_field :authors_id %>
  </div>

  <div>
    <%= form.label :books_id, style: "display: block" %>
    <%= form.text_field :books_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
